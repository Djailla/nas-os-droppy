#!/bin/sh -e
#
# rc.local
#

sed -i "s/\"port\":[0-9]*/\"port\":$RAINBOW_PORT_WEB_UI/" /opt/droppy/.config

case $1 in
  start)
    # Set the correct port information
    /opt/droppy/update_port.py $RAINBOW_PORT_WEB_UI
    droppy start -f /shares
    ;;
  stop)
    killall droppy
    ;;
esac
